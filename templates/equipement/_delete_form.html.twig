<form method="post" action="{{ path('app_equipement_delete', {'ideq': equipement.getId()}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipement.getId()) }}">
    <button class="btn">Delete</button>
</form>
